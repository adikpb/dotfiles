# Residue sweep — Round 6 (convergence), sweep 1 — 2026-08-12

**Result: NO NEW FINDINGS — 0 CODE_PATH, 0 STALE_DOC.** Third consecutive clean-ish round (r4: 0/0 + 2 frontmatter nits; r5: 1 comment nit fixed, 0/0; r6: nit re-verified, 0/0). The v2-residue ledger is converged.

## Briefed-fix verification (the round-5 nit)
- Brief: "verify bridge.py:70 comment reads `# wait=false sessions: {"last_fp", "in_flight"}`".
- **VERIFIED by content** — exact comment text present verbatim, but at **bridge.py:75**, not :70 (line 70 is now `self._serve_handle: ServeHandle | None = None`; the fixer's earlier edit shifted the map down). The brief carried a PRE-fix line number. Doctrine confirmed: for claimed-fix verification, grep the exact token/comment text and report the ACTUAL post-fix line — the briefed line is advisory only.

## Sweep ledger
Method: `grep -rniE "v2|/api/|session\.next|AgentV2|permission\.v2|question\.v2|MessageV2|CommandV2"` over plugin-owned `.py` (hermes_opencode/, tests/, scripts/, root `__init__.py`) + per-file targeted searches over plugin.yaml, pyproject.toml, README, all 21 wiki pages, .hermes/plans. **23 hits, all classified, none actionable:**

- **EXPLANATORY (16)**: v1-only banner comments (tools.py:20, bridge.py:13-14, client.py:4-5), v1/v2 contrast annotations (client.py:207 "v1 counterpart of the v2 active set", client.py:256 "not the v2 {prompt:{text},resume} wrapper"), v1-stream rationale (events.py:3,8 — subscribes v1 /event on shared EventV2 bus, avoids v2 /api/event encode fragility), ignore-path comment (events.py:242 "Everything else (session.next.*, ...) is ignored"), cursor-API rationale + upstream type name MessageV2.page (read.py:4,7), ignore-behavior tests (test_events.py:95-117: `test_v2_permission_family_ignored`, `test_v2_question_family_ignored`, session.next.* frames dispatched and asserted dropped — deliberate v1-only guards, never CODE_PATH), Hermes version stamps v2026.8.3 (approval.py:11, serve.py:8, README.md:17 — NOT opencode v2).
- **HISTORICAL-LOG (2 files)**: wiki/log.md changelog (migration entries dated); .hermes/plans/2026-08-10 skeleton plan ("v2 API preferred with v1 fallbacks") — pre-migration planning artifact, superseded by the 2026-08-11 v1-only decision.
- **Out of scope**: .slim/clonedeps/ vendored clones (v22/v23 FTS-schema refs in hermes-agent source, upstream v2 gen types) — third-party code; .venv, __pycache__, caches.
- **Wiki upstream-reference pages** (opencode-http-api, opencode-sdk, opencode-runtime, opencode-event-streams, opencode-commands, opencode-question-api, opencode-plugin-api incl. its "v2 experimental API" section): document opencode's OWN surface, not the plugin's. Banner-marked pages carry explicit markers: plugin-requirements.md:14-16 ("the plugin is V1-ONLY... v2 is documented as reference"), opencode-agent-registry.md:15-19 (blockquote: v2 path removed, reference only), opencode-permissions.md:16 ("server reference only"), opencode-session-reading.md:16-19 (v2 read surfaces "not used by the plugin"). wiki/index.md:14,33,34 blurbs describe those pages accurately.

## JSON contract note (validator bounce, trailing-prose variant)
The first final response was rejected: `Response is not valid JSON: Extra data: line 2 column 1 (char 38)` — prose trailing the JSON object. Recovery that worked: (1) re-verify the briefed nit + re-run the full sweep (the rejected output is lost; evidence must be regenerated), (2) write the human-readable report to the task-named file (`/tmp/v2_audit_r6_1_residue.md`), (3) emit ONLY the JSON object — `{"findings": [], "total_findings": 0}` — with zero prose. A ```json fence is tolerated; prose is not. This is the second observed bounce of the same contract (r2 had the malformed-JSON variant); both documented in SKILL.md pitfalls.
