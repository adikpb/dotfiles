# Hermes-OpenCode Plugin Wiki Audit — verified corpus (hermes-agent v2026.8.3)

Audit date: 2026-08-09. Clone: `.slim/clonedeps/repos/NousResearch__hermes-agent` (tag v2026.8.3, commit 3c27eb6, release 0.20.0). Wiki: `hermes-opencode-plugin/wiki/` (frontmatter-schema KB: `entities/`, `concepts/`, `sources:` frontmatter names clone paths).
Result: 6 pages audited, 2 findings (both in `concepts/hermes-approval-route.md`); all other claims verified accurate.
RE-AUDIT same day (fresh pass; wiki edited since first pass): 6 pages re-verified against the same clone — finding #1 FIXED in wiki, finding #2 STILL OPEN (lines 107-111 → 109-111), NEW finding #3 (below); all other pages/claims re-verified accurate.
ROUND-4 (final blind re-audit, re-dispatch deleg_a274d3f5, processed 2026-08-09 evening): findings #2 and #3 FIXED in wiki and re-verified against the clone; the tirith row (finding #1) independently re-verified as still correct; audit loop now TERMINAL (4 rounds applied, lint clean, 20 pages). Other round-4 outcomes live in the opencode-side reference (v2 reads empty for v1 sessions) and `sub-agent-verification` (blind-final-round technique).

## Open findings (all closed as of round 4 — audit loop terminal)
1. (FIXED in wiki; re-verified round 4 — NO action needed) tirith persistence. `hermes-approval-route.md:56` states the correct split: `choice == "always" and is_tirith` → session-only; `always` on non-tirith pattern → permanent. Matches code `tools/approval.py:3893-3899` and `:4006-4015` (inline comment :4009-4012 "tirith: session only … dangerous patterns: permanent allowed"). `has_permanent_capable` :3794. ⚠ This row was applied INVERTED from round 3 and only caught by the round-4 blind audit — see `sub-agent-verification` step 7.
2. (FIXED round 4) `hermes-approval-route.md` — the old `:109-111` sibling claim "`check_dangerous_command` via `check_all_command_guards` returns `pending_approval`" was corrected. Source verified: `check_dangerous_command` (def :3229) calls `_run_approval_gate` DIRECTLY at `approval.py:3281`; its no-callback queued shape is `status: "approval_required"` `:3161-3171` (same family as `request_tool_approval`). `status: "pending_approval"` only from `check_all_command_guards` `:3923-3935` (production terminal path `tools/terminal_tool.py:343`) and `check_execute_code_guard` `:4178`.
3. (FIXED round 4) `hermes-approval-route.md:46` — "All four delegate to `_run_approval_gate` … the single decision core" corrected. Only TWO entry points call the gate: `check_dangerous_command` (`approval.py:3281`) and `request_tool_approval` (`:3362`); the gate's own docstring :2992-2996 says it is "reused by **both**". `check_execute_code_guard` (`:4023`) and `request_elicitation_consent` (`:4263`) are parallel implementations: pending_approval `:4178`; direct `prompt_dangerous_approval` + `_await_gateway_decision` (`:4329`/`:4308`).

## Verified line-cite resolutions worth remembering (line numbers drift across tags)
- `hermes_cli/plugins.py`: `VALID_HOOKS` from ~:135 (catalog + docstring through ~:213); `ENTRY_POINTS_GROUP` :217; `_VALID_PLUGIN_KINDS` :277 = {standalone, backend, exclusive, platform, model-provider}; `PluginContext` :339; `register_tool` :410 (only 10 params — no `max_result_size_chars`/`dynamic_schema_overrides`, registry-only at `tools/registry.py:521-533`); override gate raise ~:438-445, `_tool_override_allowed` :469 (bundled trusted, else `plugins.entries.<id>.allow_tool_override`); `inject_message` :495 (CLI-only, `_interrupt_queue`/`_pending_input`); `register_cli_command` :523 / `register_command` :548; `dispatch_tool` :604; `register_context_engine` :635 (exactly one); providers 667/734/761/789/821/694/868/906; `register_platform` :950 (eager) vs `_register_deferred_platform` :1726 (bundled kind:platform only); `register_slack_action_handler` :1006; `register_auxiliary_task` :1066; `register_hook` :1177 (unknown warn-but-store); `register_middleware` :1196; `register_skill` :1217 (namespaced, NOT in `<available_skills>`); bundled backend autoload :1450, platform deferred :1465, exclusive :1418 / model-provider :1427 NOT auto-loaded, skip dirs `{memory, context_engine, platforms, model-providers}` :1355; whitelist :2108/:2116, enforced at top of `_get_pre_tool_call_directive_details` (~:2158, before hooks); `resolve_pre_tool_block` :2247, `rule_key=details.rule_key or tool_name` :2284.
- `tools/approval.py`: contextvars :69/:171/:181; `_fire_approval_hook` :96 (adds turn_id/tool_call_id); `_prepare_smart_approval_observer` :123 (redacts with force=True); `unregister_gateway_notify` :2325-2335 (unblocks ALL queued threads for session); `approve_session` :2386 in-memory only; `is_approved` :2460 (session + permanent); `approve_permanent` :2474; `save_permanent_allowlist` :2546 → top-level `command_allowlist` config key; def `load_permanent_allowlist` :2528, module-import call at :4350-4351 (wiki cited the import site — correct); `_get_approval_timeout` :2798 (default 300); `_smart_approve` :2886 called ONLY at :3749 (check_all_command_guards) and :4117 (check_execute_code_guard) — no smart branch in `_run_approval_gate` :2979; yolo/session-cache returns `{"approved": True, "message": None}` :3033-3038 & :3146-3152 & :3212 (indistinguishable outcomes); `_get_approval_callback` fallback :3040-3046; gateway reason addendum ~:3121-3128 ("Reason given by the user"); `approval_required` shape :3156-3171; prompt_toolkit fast-deny :2608-2633; `check_dangerous_command` :3229 (→ `_run_approval_gate` :3281); `request_tool_approval` :3299; reason-hash rule derivation :3349-3357 → `plugin_rule:<tool>:<sha256(reason)[:12]>` (plugin path always passes non-empty rule_key, so hash never fires there); `display_target` hardcoded `:3360`; `check_all_command_guards` :3532 (denial breaker ~:3755-3763, tirith persistence :3893-3900, redaction :3907-3909, pending_approval :3905-3927, in-code comment :3904 says "approval_required for backward compat" — code returns pending_approval); `check_execute_code_guard` :4023 (smart call :4117); `request_elicitation_consent` :4263 → plain string "accept"|"decline"|"cancel", timeout→"cancel" :4343-4346, CLI forces `allow_permanent=False` ~:4333.
- `hermes_cli/config_defaults.py:2044-2076` = `approvals.*` block: mode "smart" :2044, timeout 300 :2045, cron_mode "deny" :2046, smart_policy "" :2052-2055, denial_breaker_threshold 3 :2054-2057 (0 disables), deny [] :2068-2075 (fires BEFORE yolo bypass).
- `hermes_state_common.py:252` messages table — columns incl. `observed DEFAULT 0`, `active DEFAULT 1`, `compacted DEFAULT 0`, `api_content`, `display_kind`, `display_metadata`. `api_content` byte-fidelity sidecar doc at `hermes_state.py:6096-6102`; `SessionDB` :1791, `AsyncSessionDB` :9294, append_message :6060 / append_messages_batch :6207 / replace_messages :6619 / archive_and_compact :6691.
- `run_agent.py`: `AIAgent` :412, `chat` :7723, `_compress_context` :7115, `_flush_messages_to_session_db` :1983, steer :3221 / redirect :3257, `prefill_messages` ctor param :485.
- `agent/conversation_loop.py`: `run_conversation` :1228 (persist_user_message + persist_user_display_kind), `_restore_or_build_system_prompt` :470, model loop :1402, should_compress gate ~:1972. `_try_admit_compression_job` lives in `agent/conversation_compression.py:737` (not the loop); `compress_context` :2128; `CompressionCommitFence` :444.
- `agent/turn_context.py`: `build_turn_context` :330; user-msg append :561.
- Tools: skills model tools registered at `tools/skills_tool.py:1793` (skills_list) and :1952 (skill_view) — handler defs at :786/:962, so docs citing 1793/1952 cite the registrations; cronjob action description "create, list, update, pause, resume, remove, run" at `tools/cronjob_tools.py:1049-1051`, tool registered ~:1158-1161; `send_message` deliberately NOT an agent-callable model tool (`tools/send_message_tool.py:2106`); cron `_deliver_result` `cron/scheduler.py:1461`, `parse_schedule` `cron/jobs.py:564`; `build_skills_system_prompt` `agent/prompt_builder.py:1584` (LRU + `.skills_prompt_snapshot.json` by mtime/size, `skills.external_dirs` read-only).
- Gateway: `GatewayRunner` `gateway/run.py:5640`; `_handle_message` :14206; `_handle_message_with_agent` :16149; `MessageEvent` `gateway/platforms/base.py:2054`; `BasePlatformAdapter` :2629; `async send → SendResult` :3496.
- `hermes_cli/send_cmd.py` `cmd_send` :316; `cfg_get` `hermes_cli/config.py:2886`; whitelist consumer `agent/background_review.py:903` cleared :935; subagent auto-deny callbacks `tools/delegate_tool.py` ~:70-108 gated by `delegation.subagent_auto_approve`; middleware kinds `hermes_cli/middleware.py:20-23`, schema versions `hermes.observer.v1`/`hermes.middleware.v1` :17-18.

## Techniques that worked here
- Batch-verify: one grep per page for every symbol def, all greps in one turn; ~6 tool turns covered ~150 line cites.
- For delegation claims, `search_files` repo-wide minus `tests/` revealed the production importer (terminal_tool aliases `check_all_command_guards`).
- Binary-flagged UTF-8 wiki page → `file` + `cat -v` to continue the audit instead of skipping.
- Final report shape: findings with dual cites (code path:line + wiki line), verified-OK page list, `NO NEW FINDINGS` explicitly when clean, issues-encountered section.
- **Blind re-audit as regression detector** (round 4): an un-briefed 3-way re-audit is the highest-value final pass for a contract wiki — it re-derives every claim and catches applied-but-wrong fixes (the inverted tirith row). Pair with `sub-agent-verification` step 7.