# OpenCode source map + verified claims — v1.18.13 (tag a1053508, audit 2026-08-09)

Clone path: `.slim/clonedeps/repos/anomalyco__opencode` (vendored, tag v1.18.13, grafted).
This is the source-side map for auditing the **opencode-side** wiki pages of hermes-opencode-plugin.

## Where each surface lives (route claims → file)

| Surface | Location |
|---|---|
| V1 instance HTTP groups (root routes `/session`, `/permission`, `/question`, `/event`, `/experimental/*`, `/tui/*`, `/global/*`) | `packages/opencode/src/server/routes/instance/httpapi/groups/*.ts` (declarations) + `handlers/*.ts` (behavior) |
| V2 protocol groups (`/api/*`) | `packages/protocol/src/groups/*.ts` (declarations) + `packages/server/src/handlers/*.ts` (behavior) |
| V2 core session engine (wait/compact stubs, replay, context, history) | `packages/core/src/session.ts`, `packages/core/src/event.ts` |
| Wire/schema contracts + event manifests | `packages/schema/src/*.ts` (current) and `packages/schema/src/v1/*.ts` (legacy) |
| SDK (client spawn, groups, generated types) | `packages/sdk/js/src/{client,server,index}.ts`, `gen/sdk.gen.ts`, `gen/types.gen.ts`, `v2/*` |
| Plugin contract (types) vs plugin loader (runtime) | `packages/plugin/src/index.ts` vs `packages/opencode/src/plugin/{index,shared,loader}.ts` — SAME-NAMED FILES, cite full paths |
| Runtime service models (permission, question, tool registry) | `packages/opencode/src/{permission,question,tool}/*.ts` |
| CLI verbs | `packages/opencode/src/cli/cmd/*.ts` |

## Verified facts (spot-check path:line before re-citing)

- V1 `/event` SSE: envelope `{id, type, properties}`, per-frame `id: undefined`, first frame `server.connected`, `server.heartbeat` every 10 s (not data), terminal `server.instance.disposed` via takeUntil — `packages/opencode/src/server/routes/instance/httpapi/handlers/event.ts:12-71`. Location filter drops events whose `location?.directory !== instance.directory` or workspace mismatch (:35-39); unbounded FIFO queue (:31).
- V2 `/api/event`: frame encoded `Schema.encodeUnknownSync(OpenCodeEvent)` — `packages/server/src/handlers/event.ts:16`; `OpenCodeEvent` = `EventManifest.ServerDefinitions` only, NO `session.status`/v1 `permission.asked`/`question.asked` — `packages/protocol/src/groups/event.ts:52`; bounded 256 `Queue.dropping` + `SubscriberOverflowError` — `server/src/handlers/event.ts:9`, `packages/core/src/event.ts:152-164`; 15 s `: heartbeat` comment, no disposed frame — `server/src/handlers/event.ts:37`. Because `events.listen` fans out to every subscriber (`core/event.ts` listen impl), v1-only event types hit the v2 stream and make `encodeUnknownSync` throw → stream dies with no terminal frame.
- Full manifest includes `SessionStatusEvent`/`PermissionV1`/`QuestionV1` definitions — `packages/schema/src/event-manifest.ts:63-82` (ServerDefinitions at :57-61 excludes them).
- Session protocol (v2): list cursor `{data, cursor:{previous?,next?}}`, active GET, prompt payload `{id?, prompt:{text,files?,agents?}, delivery?, resume?}`, history limit ≤100 default 50 server-side, event `?after=` replay-then-live, interrupt idle no-op — `packages/protocol/src/groups/session.ts:83-373`; `packages/server/src/handlers/session.ts:17,80-89,333-339`; `packages/schema/src/prompt-input.ts`; `packages/core/src/event.ts:585-598` (durable = read rows > after, then live wake).
- `wait`/`compact`/`shell`/`skill` are stubs → `OperationUnavailableError` (503): `packages/core/src/session.ts:387-423` (operation literals `["move","shell","skill","switchAgent","compact","wait"]` :95-98).
- Permission model: `Request {id, sessionID, permission, patterns, metadata, always, tool?}`, `Reply = once|always|reject`, `ReplyBody {reply, message?}`, events `permission.asked`/`permission.replied` — `packages/schema/src/v1/permission.ts:27-66`; `reject`+message → `CorrectedError({feedback})` message "…with the following feedback: …", bare `RejectedError` otherwise — `packages/core/src/v1/permission.ts:7-19`; runtime ask/reply/list, last-match-wins evaluate, same-session sibling fan-out (reject branch :121-139, always branch :145-166), dispose fails pendings w/o terminal event (:54-61) — `packages/opencode/src/permission/index.ts`.
- Question: tool input `questions:[{question, header≤30, options[{label,description}], multiple?}]`; `custom` only on `Info` not `Prompt`; reply returns title `Asked N question(s)` + output with `"Unanswered"` for empty answers — `packages/schema/src/question.ts:28-44`, `packages/opencode/src/tool/question.ts:30-40`; plan tool asks build-agent confirm `custom:false` — `packages/opencode/src/tool/plan.ts:30-44`; enabled when `["app","cli","desktop"].includes(client) || enableQuestionTool` (`OPENCODE_ENABLE_QUESTION_TOOL`) — `tool/registry.ts:202`, `effect/runtime-flags.ts:41,56` (serve defaults `OPENCODE_CLIENT=cli`); dispose fails pendings w/o `question.rejected` — `question/index.ts:74-81`; v2 routes in `packages/protocol/src/groups/question.ts:20-81`.
- SDK: `createOpencodeServer` spawns `opencode serve --hostname=--port=`, env `OPENCODE_CONFIG_CONTENT`, parses "opencode server listening on <url>", defaults 127.0.0.1:4096/5000 ms; TUI flags `--project/--model/--session/--agent` — `packages/sdk/js/src/server.ts:22-134`; serve banner `packages/opencode/src/cli/cmd/serve.ts:20`; client header `x-opencode-directory` + GET/HEAD directory query rewrite — `packages/sdk/js/src/client.ts:17-54`; v2 client adds `x-opencode-workspace` + `/api/` `location[key]` params — `packages/sdk/js/src/v2/client.ts`.
- SDK groups (generated, exhaustive): global, project, pty, config, tool, instance, path, vcs, session, command, oauth, provider, find, file, app, auth, mcp, lsp, formatter, control, tui, event — `packages/sdk/js/src/gen/sdk.gen.ts` (`class Oauth` :715, `class Control` :1000). Session methods: list create status delete get update children todo init fork abort unshare share diff summarize messages prompt message promptAsync command shell revert unrevert. Tui methods: appendPrompt openHelp openSessions openThemes openModels submitPrompt clearPrompt executeCommand showToast publish.
- Plugin contract lines: `Plugin` :74, `PluginOptions` :68, `Config` :70-72, `PluginInput` :56-66, `PluginModule` :76-80, Hooks incl. `permission.ask` declared but NO trigger call-site in v1.18.13 (permissions emit events instead) — `packages/plugin/src/index.ts`; `tool()`/`ToolContext`/`AskInput`/`ToolResult` — `packages/plugin/src/tool.ts:3-54`; `readV1Plugin` :273-304, `isPathPluginSpec` (`file://`, `.`, absolute) :171-173, `checkPluginCompatibility` (engines.opencode semver) :194+, `resolvePluginId` path-requires-id :306-323 — `packages/opencode/src/plugin/shared.ts`; `getLegacyPlugins` :97-110, `applyPlugin` :112-123 — `packages/opencode/src/plugin/index.ts`; v2 experimental `@opencode-ai/plugin/v2/{promise,effect}` with `define()` + `Hooks<Spec>` registrations — `packages/plugin/src/v2/`.
- Config: `Options = Schema.Record(String, Unknown)`, `Spec = String | [String, Options]` — `packages/core/src/v1/config/plugin.ts`; `{plugin,plugins}/*.{ts,js}` auto-load — `packages/opencode/src/config/plugin.ts:16-27`; specs resolve relative to declaring config file (`resolveLoadedPlugins` — `config/config.ts:101`); `opencode plugin <module>` CLI (alias `plug`) — `packages/opencode/src/cli/cmd/plug.ts:178-201`.

- Legacy messages route & the `MessageV2` NAMING TRAP (verified 2026-08-11): `GET /session/{id}/message` (`before` + `limit`; 400 if `before` without `limit`) returns `{info, parts}` items, `info = {id, role, time:{created}, modelID|model}`. Backed by `packages/opencode/src/session/message-v2.ts` — `MessageV2.page({sessionID, limit, before}) → {items, cursor}`, `MessageV2.cursor.encode/decode` (base64url `{id,time}`), `MessageV2.get(...)`; used at `instance/httpapi/handlers/session.ts:114` (cursor.decode), `:124` (page), `:151` (get); response carries `Link` + `X-Next-Cursor` headers (`Access-Control-Expose-Headers: "Link, X-Next-Cursor"`). The "V2" suffix = message-table generation, NOT the v2 protocol — `MessageV2` is a legit v1-instance name, so a "v2" grep flags it as residue when it's accurate. **No `MessageV1` type exists anywhere in 1.18.x** (verified: zero grep matches repo-wide) — a docstring naming the legacy response "MessageV1" is wrong (real case: hermes_opencode/read.py line 42 vs its own correct line 7). Legacy prompt routes: `POST /session/{id}/message` = BLOCKING turn (`session.prompt`), `POST /session/{id}/prompt_async` = 204 no-content fork (`session.promptAsync`), same parts body.
- `/session/status` map lifecycle (verified live 2026-08-11 against 1.18.13): entry ABSENT before the forked turn starts (~1s after `prompt_async` returns), present `{type: busy}` while running, DELETED on idle — `packages/opencode/src/session/status.ts:42-47`; `get` defaults an absent session to idle (:32). So "absent at t=0" ≠ idle for wait-detection; wait for a busy sighting or use the event stream. Live e2e frame sequence: `server.connected`, `session.status`, `message.updated`, `session.updated`, `session.diff`.

1. sdk.md group list omits `Oauth` + `Control` (sdk.gen.ts:715,1000).
2. sdk.md Tui list omits `openHelp/openSessions/openThemes/openModels`.
3. sdk.md prompt-body claim includes `variant` — not in `SessionPromptData.body` (types.gen.ts:2593-2610 has messageID, model, agent, noReply, system, tools, parts); `variant` exists only on plugin `chat.message` input.

## UNVERIFIED at cutoff (resume here, do NOT claim clean on these)

- VERIFIED since the last pass (2026-08-11, round 2 of the plugin v1-migration audit): `concepts/opencode-commands.md` claims (incl. `CommandV2.Info` at `packages/schema/src/command.ts:8-15`, TUI-only list at `tips-view.tsx:172-283`), `session-status-event.ts` Status schema + `session/status.ts:32` default-idle, and the `MessageV2`/prompt_async/status-map facts added above.
- Remaining spot checks: `tool/registry.ts:120-135` (Zod→JSON-Schema) and `:179-191` (tool/tools globs), `packages/opencode/src/plugin/hooks.ts` dispatch table, `hermes_state_common.py:252` field list.