# Residue Sweep ROUND-4 (2026-08-12) — convergence

Round 4 of the v1-only migration audit loop for hermes-opencode-plugin.
Task: verify the 11 briefed Round-3 fix groups, then hunt for ANY remaining
v2 code path or stale v2/old-route doc (classify CODE_PATH / STALE_DOC /
EXPLANATORY / HISTORICAL-LOG). READ-ONLY. Result: **11/11 verified,
CONVERGENCE — zero CODE_PATH, zero STALE_DOC residue.**

## Verification (11 briefed fix groups, all VERIFIED-FIXED)

| # | Fix | Evidence |
|---|-----|----------|
| 1 | `_poll_status_idle` 0.5s→0.1s poll + comment | bridge.py:525-528 (`time.sleep(0.1)`, fast-turn-miss-window comment; saw_busy semantics kept) |
| 2 | per-session `in_flight` counter in `_delegated` | bridge.py:459-463 (prompt wait=False increments + resets last_fp); :226-239 (`_on_turn_complete` decrements on delivery, pops at ≤0, keeps entry + advances last_fp while a second overlapping turn runs); :152-157 (stop() clears `_delegated`/`_injected_questions`/`_pending_tails`) |
| 3 | `_on_idle` delegated guard hoisted above tail read | bridge.py:174-180 (foreign-idle `return` precedes `read_session` → no round-trip) |
| 4 | `_dispatch` non-dict `properties` → {} | events.py:177-178 (`isinstance(raw_props, dict)` guard); status-dict guard :190 |
| 5 | tail tool limit slices BUFFERED tails | tools.py:265-266 (`entries = entries[: _coerce_limit(...)]` after consume_tail fast path) |
| 6 | schema descriptions (opencode_command params; opencode_prompt.wait cross-directory poll doc) | tools.py:172-183; :88-98 |
| 7 | README prompt row documents cross-directory wait=true | README.md:87 |
| 8 | log.md superseded banners on both flagged [2026-08-10] entries | wiki/log.md:33-35 and :84-86 ("Superseded by the 2026-08-11 v1-only migration entry above") |
| 9 | session-reading diag2_e2e.py annotation | wiki/concepts/opencode-session-reading.md:72-73 ("the diag2_e2e.py probe was removed in the 2026-08-11 v1-only migration") |
| 10 | test fakes record full kwargs + 4 new tests + /proj assertions | test_bridge.py:55-63, test_tools.py:60-61 (record (sid, text, agent, model, directory)); new: test_bridge.py:285 `test_overlapping_wait_false_prompts_both_notify`, :300 `test_prompt_forwards_config_agent_model_and_directory`, test_events.py:178 `test_dispatch_tolerates_malformed_status`, :188 `test_dispatch_tolerates_non_dict_properties`; /proj asserts test_bridge.py:347,376 / test_tools.py:106,115,214; suite **161 passed + 1 subtest** |
| 11 | __pycache__ purge | regenerated by test runs; gitignored (.gitignore:10) — not a finding |

Moving-target guard: mtimes static across the run (bridge.py/events.py/tools.py
1786506064/1786506064/1786506080 before and after). ruff not in .venv (no
module) — static grep evidence + green suite used instead.

## Classification of every remaining v2 mention

- **CODE_PATH (0)** — all `.py` v2 mentions are comments/docstrings explaining
  v2 is deliberately unused (client.py:4-5,207,256; bridge.py:13-16,444;
  tools.py:20; read.py:4; events.py:1-9,208). No `/api/*` route called in
  executable code. scripts/e2e_smoke.py: zero v2/engine/active_sessions hits
  (round-1 finding stays closed). plugin.yaml / pyproject.toml / README.md:
  zero v2 mentions.
- **STALE_DOC (0)** — every wiki page with v2 content is either
  banner-marked (plugin-requirements :14-19, session-reading :14-20,
  agent-registry :15-19, permissions :14-17) or documents opencode's own
  server surface non-prescriptively (http-api — deliberately untouched per
  log.md:12 — event-streams, question-api, commands, message-injection,
  sdk, plugin-api, runtime, index).
- **EXPLANATORY** — negative tests asserting v2 families ignored
  (test_events.py:95-99, :107-111, :113-120 incl. session.next.stop frame),
  test_read.py:148-151 (`scope="context"` must RAISE ValueError), banner
  pages' inline "reference only, dropped 2026-08-11" labels, R5 "No /api/*
  route is used anywhere in the plugin" (plugin-requirements.md:241-250).
- **HISTORICAL-LOG** — wiki/log.md dated entries (2026-08-09/10) predating
  the migration, incl. the two now-banner-marked ones. Rule: an append-only
  chronological log is accepted as history UNLESS an entry reads as
  current-state; those get the superseded banner (that is exactly what
  round-3's STILL-PRESENT finding was, and it is now closed).

## New finding (1 nit, no v2 residue)

- bridge.py:70 — `# wait=false sessions: {"last_fp"}` under-describes
  `_delegated` entries that now carry `in_flight` (added by fix 2).
  **Lesson: comment-drift is a fix-surface class** — a fix that changes a
  data structure must update the inline comment that describes it; check
  the describing comments as part of fix verification, not just the code
  paths. Fix: extend the comment to `{"last_fp", "in_flight"}`.

## Convergence-round method

- 'NO NEW FINDINGS is a valid and valuable result' — say it plainly when
  the fix groups verify and the sweep is clean; do not manufacture findings
  to look busy.
- Run the FULL deleted-name grep list (V2Collapser, detect_engine,
  _event_seq, scope=context, _last_stop, _fallback_v1, _deny_locked_out,
  fallback:"v1", active_sessions, prompt_legacy, create_session_v1,
  session.next.stop, permission.v2, question.v2, /api/permission,
  engine="v2", opencode_questions, question_list, ...) across code AND
  scripts AND tests, and classify EVERY hit — an unclassified hit is an
  unfinished audit.
- Negative tests that exercise deleted surface are EXPLANATORY, not
  residue — they pin the deletion.
- Report JSON counts only new/still-present issues; the verification table
  lives in the report file, not the JSON (machine-validated output contract
  = JSON only, no trailing prose).
